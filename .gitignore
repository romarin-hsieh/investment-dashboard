node_modules
dist
dist-deploy
.vscode
.kiro
.env
*.log
# Pre-computed data lives in the separate data repo (ADR-0008). It is generated
# locally (npm run seed-data) or by the nightly workflows, but never committed here.
public/data/
node_modules

# Backup files
*.backup
*.json.backup

# Bundle analyzer output (rollup-plugin-visualizer)
/stats.html
/stats.json
