# 2026-08-07 Adversarial round-2 audit + PM foundation — progress log

> Working state journal for the 2026-08-07 goal. Updated at every segment boundary and
> pushed, so a lost session can resume from here. Newest entries at the top of §Log.
>
> **Goal (as set):** adversarial + AI-slop audit of layout structure, system design,
> style inconsistency (outer pages vs system pages, button heights), and copy
> (unnatural / unprofessional / non-neutral / off-domain wording) → fill the PM doc
> gaps (user stories, user flows, state machines, wireframes, BDD/DDD gap analysis)
> → build a Given-When-Then framework → fix confirmed findings with before/after
> screenshots and adversarial re-verification. Push at every segment boundary.

## Segment plan

| # | Segment | Branch | PR | Status |
|---|---------|--------|----|--------|
| 1 | Baseline screenshots (before) + adversarial audit round 2 | `docs/audit-2026-08-baseline` | — | **in progress** |
| 2 | PM foundation docs (user stories, flows, state machines, wireframes) | `docs/pm-foundation` | — | pending |
| 3 | BDD/DDD gap analysis + Given-When-Then framework | `test/gwt-framework` | — | pending |
| 4 | Fixes: style unification (buttons etc.) with after-screenshots | `fix/ui-consistency` | — | pending |
| 5 | Fixes: copy / AI-slop | `fix/copy-slop` | — | pending |
| 6 | Final adversarial pass over the whole change-set | — | — | pending |

## Standing decisions

- **Baseline commit:** `88311ce30` (main, 2026-08-07).
- Screenshots: `scripts/audit-screenshots.mjs` → 7 gated routes × light/dark, 1280×800
  viewport JPEG, third-party hosts blocked for determinism. Before-set committed under
  `docs/audits/2026-08-07-screenshots/before/`; each layout-fix PR adds its after-set.
- Audit method mirrors 2026-07-20: independent lenses, then a skeptic pass; only
  verified findings ship. Round 2 additionally checks which of the 18 round-1 findings
  are still open.
- Previous audit: [2026-07-20-adversarial-uiux-audit.md](2026-07-20-adversarial-uiux-audit.md).

## Log

- **2026-08-07 · S1 first-hand findings recorded** (from the committed before-set, my own
  review, pending skeptic adjudication — merge into the audit doc with the lens results):
  - FH-1 Four different page-header patterns across 7 routes (market: eyebrow+title+
    subtitle+rule · stock-overview: title+subtitle only · system-manager/technical-manager:
    CENTERED title+subtitle · auto-update-monitor: bare left title+rule · settings: tight
    title+subtitle, no rule).
  - FH-2 ≥8 distinct button treatments; heights ~32→~48px (technical-manager 4 large
    navy/yellow/teal/grey · auto-update small navy/grey/amber + slate/green top pair ·
    system-manager slate/mustard · outer red 重試 / slate pills).
  - FH-3 zh-TW terminology violates own GLOSSARY: 緩存/快取 (both in ONE card on
    technical-manager: 「每日緩存」header vs 「記憶體快取」row), 數據/資料, 元數據/中繼資料,
    刷新/重新整理, 調度器 (unglossaried; 排程器 is the Taiwan norm).
  - FH-4 Route-name drift: nav 個股總覽 vs breadcrumb/back-button 股票總覽; same indices
    widget titled 市場指數 (market page) vs 大盤指數 (stock page).
  - FH-5 Emoji-as-icon on system-manager cards (🚀🕐📈) — AI-slop tell; outer pages are icon-free.
  - FH-6 Same failure class, three copy variants + two visual treatments (stock-detail
    圖表無法載入…重試 no border vs 無法載入此元件…red border; market pages terse 載入失敗).
  - FH-7 Settings footer 「市場數據由以下提供 TradingView」 — translationese + 數據; © year now
    2026 (round-1 S7 likely fixed).
  - FH-8 When status fetches fail, system-manager cards assert 過期/從未/0 (definite claims)
    instead of an unknown/error state; errors only visible in the log panel.
  - FH-9 auto-update-monitor inline note 「註：實際預計算需要在伺服器端執行。」 admits the
    adjacent control can't do what it implies client-side.
  - FH-10 緩存預熱 card 追蹤股票 24 (= VITE_STOCK_SYMBOLS count) vs 元數據 card 140 —
    two unexplained "tracked" universes side-by-side (clarity, not fabrication).
  - Capture caveat: third-party hosts blocked ⇒ market-overview indices + stock-detail
    TradingView areas captured in their (real) error states; noted for after-comparisons.

- **2026-08-07 · S1 started.** Branch created off `88311ce30`. Screenshot script written
  (`scripts/audit-screenshots.mjs`), E2E build running, three audit lenses (style /
  copy / system design) dispatched in parallel. Next: capture before-set, verify lens
  findings with a skeptic pass, write `2026-08-07-adversarial-round2-audit.md`.
