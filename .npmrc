# vue-i18n precompiler (@intlify/unplugin-vue-i18n) pulls a stale Vue 2-era
# optional peer (vue-i18n-bridge -> @vue/composition-api). It is unused on Vue 3;
# legacy-peer-deps lets npm install / npm ci resolve past that optional conflict.
legacy-peer-deps=true
